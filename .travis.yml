language: generic 
sudo: required
services: 
  - docker

before_install:
  - docker build -t roquesanjuan/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true roquesanjuan/docker-react npm run test